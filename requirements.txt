# ══════════════════════════════════════════════════════════════════════════════
# Vasudha — Core Requirements
# Optimized for Google Colab Free Tier (T4 GPU, 15GB VRAM)
# ══════════════════════════════════════════════════════════════════════════════

# ── PyTorch (install with CUDA separately on Colab) ───────────────────────────
# On Colab: torch is pre-installed. For local: pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.2.0
torchvision>=0.17.0

# ── Hugging Face ecosystem ─────────────────────────────────────────────────────
transformers>=4.44.0
accelerate>=0.33.0
peft>=0.12.0
trl>=0.10.0
datasets>=2.21.0
tokenizers>=0.19.0
huggingface-hub>=0.24.0

# ── Quantization (QLoRA / 4-bit loading) ─────────────────────────────────────
bitsandbytes>=0.43.0

# ── Configuration ──────────────────────────────────────────────────────────────
hydra-core>=1.3.2
omegaconf>=2.3.0

# ── Logging & monitoring ───────────────────────────────────────────────────────
rich>=13.7.0
tensorboard>=2.17.0

# ── Scientific computing ───────────────────────────────────────────────────────
numpy>=1.26.0
scipy>=1.13.0
einops>=0.8.0

# ── Data utilities ─────────────────────────────────────────────────────────────
tqdm>=4.66.0
safetensors>=0.4.4

# ── Type hints ─────────────────────────────────────────────────────────────────
typing-extensions>=4.12.0

# ── Desktop app runtime ────────────────────────────────────────────────────────
# jinja2 renders each GGUF's own chat template, which is what lets the harness
# run models other than the bundled one. psutil supplies the physical-core count
# used for n_threads — hyperthreads measurably hurt llama.cpp throughput, so the
# distinction matters (see the table in app/backends.py).
jinja2>=3.1.0
psutil>=5.9.0

# ── Optional: real browsing ────────────────────────────────────────────────────
# browse_tool reads pages after their JavaScript has run, which fetch_tool
# cannot. Optional on purpose: playwright downloads a ~150 MB browser of its
# own, which would contradict the desktop app's "no bundled browser" property.
# Without it the tool is simply not offered and fetch_tool remains the fallback.
#   pip install playwright && playwright install chromium
# playwright>=1.40.0
